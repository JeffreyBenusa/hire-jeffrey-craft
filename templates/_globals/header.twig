<nav class="bg-gray-800 fixed top-0 z-20 w-full">
    <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
        <div class="relative flex h-16 items-center justify-between">
            <div class="absolute inset-y-0 left-0 flex items-center sm:hidden">
                <!-- Mobile menu button-->
                <button type="button" class="relative inline-flex items-center justify-center rounded-md p-2 text-gray-400 hover:bg-gray-700 hover:text-white focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white"
                        aria-controls="mobile-menu" aria-expanded="false" aria-label="Open menu">
                    <span class="absolute -inset-0.5"></span>
                    <span class="sr-only">Open main menu</span>
                    <svg class="block size-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true" data-slot="icon">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                    </svg>
                    <svg class="hidden size-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true" data-slot="icon">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18 18 6M6 6l12 12" />
                    </svg>
                </button>
            </div>
            <div class="flex flex-1 items-center justify-center sm:items-stretch sm:justify-between">
                <div class="flex shrink-0 items-center">
                    <a href="/" class="h-8 w-auto">
                        <img class="h-8" src="{{ craft.vite.asset("src/img/hirejeffrey/HireJeffrey-Long.svg") }}" alt="Hire Jeffrey - Web Development & Engineering Leadership" title="Hire Jeffrey - Web Development & Engineering Leadership">
                    </a>
                </div>
                <div class="hidden sm:ml-6 sm:block">
                    <div class="flex space-x-4 text-gray-300">
                        <a href="{{ url('projects') }}" class="transition rounded-md px-3 py-2 text-sm font-medium  hover:bg-gray-700 hover:text-white {{ _self.isActive('projects') | trim }}">Projects</a>
                        <a href="{{ url('about-me') }}" class="transition rounded-md px-3 py-2 text-sm font-medium  hover:bg-gray-700 hover:text-white {{ _self.isActive('about-me') | trim }}">About Me</a>
                        {% if currentUser %}<a href="{{ url('blog') }}" class="transition rounded-md px-3 py-2 text-sm font-medium hover:bg-gray-700 hover:text-white {{ _self.isActive('blog') | trim }}">Blog</a>{% endif %}
                        <a href="{{ url('contact') }}" class="transition rounded-md px-3 py-2 text-sm font-medium hover:bg-gray-700 hover:text-white {{ _self.isActive('contact') | trim }}">Contact</a>
                        <a href="{{ globals.resume.one().url }}" class="transition rounded-md px-3 py-2 text-sm font-medium hover:bg-gray-700 hover:text-white" target="_blank" download>Resume</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Mobile menu, show/hide based on menu state. -->
    <div class="hidden" id="mobile-menu" aria-expanded="false">
        <div class="space-y-1 px-2 pb-3 pt-2">
            <a href="{{ url('projects') }}" class="block rounded-md px-3 py-2 text-base font-medium text-gray-300 hover:bg-gray-700 hover:text-white">Projects</a>
            <a href="{{ url('about-me') }}" class="block rounded-md px-3 py-2 text-base font-medium text-gray-300 hover:bg-gray-700 hover:text-white">About Me</a>
            {% if currentUser %}<a href="{{ url('blog') }}" class="block rounded-md px-3 py-2 text-base font-medium text-gray-300 hover:bg-gray-700 hover:text-white">Blog</a>{% endif %}
            <a href="{{ url('contact') }}" class="block rounded-md px-3 py-2 text-base font-medium text-gray-300 hover:bg-gray-700 hover:text-white">Contact</a>
            <a href="{{ globals.resume.one().url }}" class="block rounded-md px-3 py-2 text-base font-medium text-gray-300 hover:bg-gray-700 hover:text-white" target="_blank" download>Resume</a>
        </div>
    </div>
</nav>

{% macro isActive(uri) %}
    {% if craft.app.request.getPathInfo() == uri %}
        bg-white text-accent-dark
    {% else %}
        text-gray-300
    {% endif %}
{% endmacro %}