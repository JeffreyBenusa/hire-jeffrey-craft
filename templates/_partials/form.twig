{% set params = {
    submitClasses: "bg-accent-dark text-white hover:bg-accent"
}|merge(params|default({})) %}

{{ freeform.form("contactMe", {
    buttons: {
        attributes: {
            submit: { class: params.submitClasses ~ " font-bold py-2 px-4 rounded mr-2" },
        }
    }
}).render() }}

